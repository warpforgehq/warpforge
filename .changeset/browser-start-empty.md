---
"warpforge": patch
---

The browser's new-tab page no longer looks empty and stranded when a project has no running services. It now shows a centered prompt to type a URL or start a service, and once services are running they appear centered as a tidy quick-link list.
