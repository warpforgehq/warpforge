---
"warpforge": patch
---

The in-app browser now has a single address bar. The start page used to show its own search box on top of the toolbar's, which was confusing and only opened links; now typing anywhere goes through the one address bar at the top, and the cursor lands there automatically on a new tab.
