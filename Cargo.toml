[workspace]
members = ["crates/warpforge-protocol"]
# The Tauri app has heavy system-library deps (webkit2gtk etc.) — kept out of the
# main workspace so `cargo build` for the daemon/TUI never requires a GUI toolchain.
exclude = ["desktop/src-tauri"]
resolver = "2"

[package]
name = "warpforge"
version = "0.19.3"
edition = "2021"

[[bin]]
name = "warpforge"
path = "src/main.rs"

[dependencies]
ratatui = "0.29"
crossterm = { version = "0.28", features = ["event-stream"] }
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
portable-pty = "0.8"
vt100 = "0.15"

serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
anyhow = "1"
uuid = { version = "1", features = ["v4"] }
dirs = "5"
futures = "0.3"
bytes = "1"
clap = { version = "4", features = ["derive"] }
tokio-tungstenite = "0.24"
rusqlite = { version = "0.32", features = ["bundled"] }
sqlite-vec = "0.1.9"
# Optional local embeddings (memory v1.5). `ort-load-dynamic` keeps the build
# offline-capable (no ONNX Runtime binary download at build time); the
# all-MiniLM-L6-v2 model (~80 MB) is fetched lazily at runtime and falls back
# to FTS-only when unavailable.
fastembed = { version = "5", default-features = false, features = ["hf-hub-native-tls", "ort-load-dynamic"] }
warpforge-protocol = { path = "crates/warpforge-protocol" }
base64 = "0.22"
chrono = "0.4"
walkdir = "2"
sha2 = "0.10"
async-trait = "0.1"
rfd = "0.15"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
cron = "0.17.0"
chrono-tz = "0.10.4"
iana-time-zone = "0.1"
tempfile = "3"

# `dup2` for pointing the daemon's own stdin at /dev/null (see
# `daemon::server::detach_stdin`); std has no way to replace fd 0.
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
