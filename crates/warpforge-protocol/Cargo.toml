[package]
name = "warpforge-protocol"
version = "0.19.2"
edition = "2021"
description = "Wire types for the warpforge daemon API (WebSocket, JSON envelope). Shared by the daemon and every client (TUI, desktop shell)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
